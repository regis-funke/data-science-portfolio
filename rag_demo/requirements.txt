# Pinned to the versions this project was built and evaluated against.
# LangChain's 1.x packages move quickly; unpinned installs of a year-old demo
# tend not to run.
langchain==1.3.14
langchain-core==1.5.3
langchain-openai==1.4.1
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
chromadb==1.5.9
openai==2.53.0
pypdf==6.14.2
python-dotenv==1.2.2

# Phase 5: API
fastapi==0.141.1
uvicorn==0.52.1

# Phase 6: agent. Pulled in by langchain 1.x already, pinned here because
# src/agent.py depends on it directly.
langgraph==1.2.10
